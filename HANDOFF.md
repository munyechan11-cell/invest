# 인수인계 — 다음 사람이 알아야 할 것

이 문서 하나만 읽고 이어받을 수 있게 씁니다. 코드가 이미 말하는 것(구조,
API 목록)은 안 적고, **코드를 읽어도 모르는 것**만 적습니다.

이 문서의 커밋·테스트 수는 고정값으로 믿지 마세요. 이어받을 때
`git rev-parse HEAD`와 전체 테스트를 다시 실행해 현재 상태를 확인합니다.

> **구 첨부본 폐기:** 2026-08-24에 전달된 `HANDOFF.md` 사본은 과거
> `7d59d0b` 상태와 반박 전 브랜치 적용 절차를 담은 구본입니다. 그 사본의
> 커밋·테스트 수, `git apply`, pending 브랜치 상태를 따르지 마세요. 이 저장소의
> 최신 `HANDOFF.md`와 `docs/pending_review.md`만 인계 기준으로 사용합니다.

---

## 0. 이 서비스는 **실거래 기능**을 포함합니다

여섯 전략이 `mode: live` 입니다. 현재 봇이 켜져 있다는 뜻은 아니므로 운영
화면과 `/api/health`에서 매번 다시 확인합니다. 다만 시작 버튼을 누르면 **진짜
주문이 나가고 진짜 돈이 움직입니다.** 토스에는 모의투자 환경이 없어서 되돌릴
수단이 없습니다.

| 전략 | 하루 한도 |
|---|---|
| 국내 · 수급 추종 (토스) | 50만원 · 10건 · 손실 5만원 |
| 국내 · AI 데스크 (토스) | 50만원 · 10건 · 손실 5만원 |
| 국내 · AI 데스크 + 수급 (한투) | 50만원 · 10건 · 손실 5만원 |
| 미국 · 상대강도 상위 (토스) | $500 · 10건 · 손실 $50 |
| 미국 · AI 데스크 (토스) | $500 · 10건 · 손실 $50 |
| 코인 · 추세추종 4시간봉 | $300 · 10건 · 손실 $30 |

데모(백테스트)는 `demo`, `demo_flow`, `kr_equity`, `us_equity` 넷입니다.

**작업 중 지킬 것**

- `quant live` 를 직접 실행하지 마세요. 테스트로 충분합니다.
- 실제 증권사 엔드포인트를 호출하지 마세요. 토스 공식 스펙 사본이
  `scratchpad/toss_openapi.json` 에 있고, 목(mock)으로 검증하면 됩니다.
- LLM 을 실제로 부르지 마세요(크레딧). `POST /api/evaluate` 의 성공 경로가
  그 길입니다.
- `.env` 내용을 출력하거나 복사하지 마세요.
- **장이 열려 있는 동안 배포하지 마세요.** 이유는 3번에 있습니다.

---

## 1. 화면은 파일 셋입니다

```
quant/api/static/app.css      스타일 전부  ← 디자인은 여기만
quant/api/static/index.html   마크업 + 동작
quant/api/static/chart.js     캔들 차트
```

파일 크기는 계속 바뀝니다. 문서에 적힌 숫자를 복사하지 말고 필요할 때
`wc -m quant/api/static/app.css quant/api/static/index.html`로 현재 값을 봅니다.

### 절대 깨면 안 되는 계약 셋

**(a) 스크립트가 붙잡는 id 셋.** 이름을 바꾸면 그 기능이 조용히 멈춥니다.
고정 목록은 곧 낡으므로 이 문서에 복제하지 않습니다. `tests/test_ui_ids.py`가
현재 스크립트의 `getElementById(...)`와 `$("#...")` 참조를 마크업과 대조합니다.
바꿔야 하면 마크업·스크립트를 함께 고치고 이 테스트를 실행하세요.

**(b) 상태 class 셋.** 스크립트가 켜고 끕니다. CSS 에서 이름을 바꾸면
화면이 상태를 표현하지 못합니다.

| class | 뜻 |
|---|---|
| `running` | 봇이 돌고 있음 (전략 선택기를 감춤) |
| `live` | **실거래 중** — 배지가 붉게 뛰고 상단 띠가 뜸 |
| `anon` / `showauth` | 로그인 전 / 로그인 화면 |
| `nodesk` | 이 전략엔 AI 데스크가 없음 |
| `idle` / `talking` | 발언 대기 / 재생 중 |
| `up` / `down` | 상승 / 하락 |
| `demoing` / `touring` | 데모 재생 / 첫 방문 안내 |
| `hidden` / `on` / `active` | 일반 토글 |

**(c) `<script>` 블록은 통째로 하나입니다.** 문법 오류 하나 —
같은 블록에 `const` 를 두 번 선언하는 것만으로도 — 스크립트가 **한 줄도**
실행되지 않고 화면이 백지가 됩니다. 콘솔에는 한 줄만 뜹니다. 실제로 겪었고
20분을 잃었습니다. `tests/test_dashboard_script_parses.py` 가 막습니다.

### 색 규칙

**방향과 상태는 다른 색입니다.**

- `--up` 빨강 / `--down` 파랑 — **한국식**, 등락 전용
- `--ok` / `--warn` / `--bad` — 성공·경고·실패

겸용하면 한국식으로 바꾸는 순간 "저장 완료" 가 빨강이 되고 "오류" 가
파랑이 됩니다. 실제로 한 번 그랬습니다.

새 토큰은 `:root` 에 먼저 정의하세요. **정의 없는 `var()` 는 오류 없이 그
속성만 사라집니다** — 배경이 통째로 투명해진 적이 있습니다.

### 디자인 근거와 검증 경계

픽셀 트레이딩 플로어의 분위기와 한눈에 보이는 조직 배치는 아래 두 Reel을
벤치마킹했습니다.

- [Reel 1](https://www.instagram.com/reel/Daw0jd6yP5R/)
- [Reel 2](https://www.instagram.com/reel/DbZobgLTBUi/)
- [Figma 릴리스 감사 파일](https://www.figma.com/design/tpl2jkD339wvYkjKMEQWud)

`tests/test_ui_readability.py`와 `tests/test_ui_structure.py`는 본문 가독성과 모바일
DOM 순서를 자동 검증합니다. 픽셀 감성과 벤치마크 대비 우수성은 사용자가 실제
브라우저에서 직접 비교하기 전에는 입증됐다고 판정하지 않습니다.

---

## 2. 이 코드베이스가 지키는 원칙

고칠 때 이 셋만 지키면 나머지는 코드가 설명합니다.

**없는 숫자를 지어내지 마세요.** 모르면 표시하지 않는 편이 낫습니다. 화면에
자신 있게 뜬 틀린 값이 빈칸보다 훨씬 나쁩니다. 토스 계좌 응답은 회계상
예수금과 같은 legacy `cash`를 제공하지 않으므로 `cash: None`을 유지하지만,
통화별 공식 `cashBuyingPower`는 `cash_buying_power`로 따로 받습니다. 화면의
운용 가능 자산 `investable_assets`는 **같은 통화의** `cash_buying_power +
holdings market_value`이며, 통화를 섞거나 설정의 `starting_cash`·80만원 같은
고정값으로 대체하지 않습니다. 실제 응답이 없거나 잘못되면 0을 발명하지 말고
조회 실패 이유를 표시합니다.

**조용히 실패하지 마세요.** 안 되면 **왜** 안 되는지 화면에 쓰세요. 이 저장소의
버그 절반이 "아무 일도 안 일어나는데 이유가 어디에도 없음" 이었습니다.
서버는 이유를 만들어 두고 화면이 안 읽는 패턴이 반복됐습니다
(`disabled_reason`, `desk_note`, `market.minutes_to_open`).

**테스트는 성질을 검사하세요.** 구현식을 베끼면 구현이 틀려도 통과합니다.
고치기 전 코드로 되돌려 실제로 실패하는지 확인하세요. 실제로 "문자열이 있는가"
만 보는 테스트를 썼다가, 수정을 통째로 되돌려도 통과하는 것이 드러났습니다.

주석은 **한국어**로, "무엇을" 이 아니라 **"왜"** 를, 특히 **"언제 이게 안
통하는가"** 를 적습니다.

---

## 3. 재시작·복구의 현재 안전 계약

### Toss 실거래의 하루 한도는 계좌 단위로 이어집니다

현재 구현은 전략 이름만 보고 새 원장을 여는 과거 경로를 쓰지 않습니다.

- 시작 전 DB owner claim을 잡고, 같은 전략·`live`·통화·한도 시간대의 **정확한
  run id**만 원자적으로 재개합니다. 재시작 전후 주문 수·거래대금·수수료·실현
  손익 원장은 같은 run에서 이어집니다.
- 같은 Toss 계좌에서 한도 reset 시각 전 다른 전략으로 바꾸면 빈 원장을 만들지
  않고 시작을 거부합니다. 전략을 바꿔 당일 한도를 우회할 수 없습니다.
- `order_submitted`와 체결·종료 이벤트를 저장 원장과 대조합니다. 저장 실패,
  누락된 제출 연결, 이벤트 수수료가 원장보다 크거나 이벤트 실현손실이 원장보다
  나쁜 상태처럼 한도를 정확히 증명할 수 없으면 새 한도를 주지 않고 fail closed
  합니다.
- 현재 한도 구간에 걸리는 legacy row, 깨진 JSON·통화·시간대·한도 scope,
  미래 시각처럼 안전한 이어받기를 증명할 수 없는 값도 임의로 0부터 시작하지
  않습니다. 이미 보수적 만료 경계를 지난 과거 증거만 새 시작을 막지 않습니다.

정상 종료를 증명하지 못한 Toss 실거래 run은 격리되어 자동 재개되지 않습니다.
계정/설정 화면의 복구 카드는 봇이 꺼진 상태에서만 다음 절차를 받습니다.

1. 토스 앱에서 미체결·당일 체결·보유 수량·현금·당일 손실 다섯 항목을 각각 대조
2. 10~500자의 운영 사유 입력
3. 서버가 내려준 acknowledgement 문구를 정확히 입력
4. 현재 `config_path`와 정확한 run id가 그대로일 때만 보존 처리

보존 처리는 기존 run과 감사 기록을 **삭제하지 않습니다.** 성공해도 봇을
자동으로 시작하지 않으며, 당일 한도 초기화 우회를 막기 위해 서버가 안내한
다음 KST 날짜의 `next_start_allowed_at` 전에는 새 실거래 시작을 거부합니다.
그 시각 이후 사람이 다시 시작해야 새 run이 열립니다.

이 계약은 현재 코드에서 새로 설계한 것입니다.
`origin/pending/daily-cap-survives-restart`를 수정본으로 간주하거나 일부라도
merge·cherry-pick하지 마세요. 그 브랜치는 한도 카운터가 아니라 halt만 옮기고
scope와 0-cap 우회가 무너져 최종 반박됐습니다.

### 워크트리에 있던 것을 전부 브랜치로 옮겼습니다

세션 임시 폴더에 있으면 세션이 끝날 때 경로째 사라집니다. 전부
`origin/pending/*` 으로 푸시했습니다. `git fetch` 하면 받아집니다.

| 브랜치 | 무엇 | 검증 판정 |
|---|---|---|
| `pending/toss-fees` | 토스 수수료·거래세 0원 | **반박됨 · 머지 금지** |
| `pending/pnl-split-by-mode` | 모의·실거래 손익 혼합 | **반박됨 · 머지 금지** |
| `pending/daily-cap-survives-restart` | **하루 한도 재시작 초기화** (위) | **반박됨 · 머지 금지** |
| `pending/manual-pending-orders` | 수동 대기주문이 화면에 안 보이고 취소 불가 | **반박됨 · 머지 금지** |
| `pending/kis-unfinished-daily-bar` | KIS 가 안 끝난 당일 봉을 확정봉으로 줌 | **반박됨 · 머지 금지** |
| `pending/backtest-trade-counting` | 성적표의 거래수·승률이 분할매도에 지배됨 | **반박됨 · 머지 금지** |
| `pending/desk-metering` | 봇 데스크 심의가 요금제 한도를 안 거침 | **반박됨 · 머지 금지** |
| `pending/fx-layer` | 통화 환산 계층 (아래 5번) | **반박됨 · 머지 금지** |
| `pending/toss-websocket` | 실시간 시세 | **반박됨 · 머지 금지** |

브랜치들은 과거 `4126f58`에서 갈라졌지만, 그 diff나 patch를 현재 main에
적용하는 것은 검증 절차가 아닙니다. 현재 main에서 증상을 다시 재현하고 새
수정을 별도로 설계해야 합니다.

**검증 결과: 아홉 건 전부 반박이 나왔습니다.** 통과 0건.

브랜치별 반박이 `docs/pending_review.md` 에 있습니다 — 전부 재현된 것이고,
재현 방법과 관측값이 함께 적혀 있습니다. **머지 전에 그 문서의 해당 절을
먼저 읽으세요.**

이 일곱 건은 이번이 3라운드입니다. 매 라운드 **원래 결함 진단은 맞았는데
고치면서 새 결함을 만들었습니다.** 그 패턴이 이번에도 반복됐습니다. 그러니
"이미 세 번 고쳤으니 괜찮겠지" 가 아니라 그 반대로 읽으세요.

셋은 이미 main 에서 고쳤습니다(`944f41b`) — 배포된 코드에 걸려 있던
것들이라 먼저 처리했습니다:

- 모드 전환 중 요청이 실패하면 실거래 체결이 "모의매매" 표에 섞이던 것
- 봇이 꺼져 있으면 실거래만 한 계정에 0 이 뜨던 것
- 취소 경로에서 체결이 통째로 사라지던 것

**다시 고치기 전에 하세요**

1. `docs/pending_review.md` 의 해당 절을 읽으세요.
2. 현재 main에서 문서의 재현 절차와 관측값을 다시 확인하세요.
3. 과거 diff는 실패 패턴을 이해하는 참고 자료로만 읽고 적용하지 마세요.
4. 전체 테스트를 돌리세요.
5. **수정을 되돌려서 테스트가 실제로 실패하는지 보세요.** 되돌려도 통과하는
   테스트는 아무것도 지키지 않습니다 — 이 저장소에서 실제로 두 번 나왔습니다.

---

## 3-A. 한 계좌에 에이전트 넷 — 무엇이 하나여야 하는가

한 사람이 성향이 다른 봇을 최대 넷까지 같은 계좌에서 굴릴 수 있습니다(공격형
5만원 + 보수형 5만원처럼). 이 기능을 고칠 때 알아야 할 것은 기능 자체가 아니라
**무엇이 넷이 되면 안 되는가** 입니다.

| 계좌에 하나 (`GroupTrader`·`AccountGateway`) | 에이전트마다 (`LiveTrader`) |
|---|---|
| 상태 DB 열기와 소유권 주장 | 워밍업 · 시세 이력 |
| 증권사 연결과 종료 | 실행 행(`runs`) 시작/재개 |
| 계좌 자본의 진실과 배분 | 포지션·잠금·핀 복원 |
| 계좌 단위 하루 한도 | 엔진 시작 · 봉마다 판단 |
| 미귀속 채택 · 합계 불변식 | 체결 정산 · 최종 상태 저장 |

**증권사는 종목별 합계 수량 하나만 알려줍니다.** 공격형 10주와 보수형 10주는
계좌에서 구별되지 않고, 그 구별은 오직 우리 원장에만 있습니다. 원장이 틀리면
틀렸다고 알려 줄 바깥 세계가 없으므로, 매 동기화마다 확인합니다:

```
Σ 에이전트 슬리브(종목) + 미귀속(종목) == 증권사 합계(종목)
```

어긋나면 **그룹 전체** 를 멈춥니다. 어느 슬리브가 틀렸는지 알 방법이 없기
때문입니다 — 한 명만 멈추는 것은 "누가 틀렸는지 안다" 는 뜻인데 우리는 모릅니다.

### 고칠 때 절대 하지 말 것

이 목록은 전부 **실제로 재현해 본 사고** 입니다. 공통점은 조용하다는 것 —
예외도 로그도 없이 그냥 틀린 답을 냅니다.

- **엔진을 세운 뒤 `engine.brokerage = sleeve` 로 바꿔치기.** 슬리브의 `budget`
  과 `portfolio` 가 `None` 으로 남고 `ctx.brokerage` 는 계좌 어댑터를 가리킨 채
  남습니다. `build_engine(brokerage=...)` 로 생성자에 넣으세요.
- **슬리브의 `positions()` 가 계좌 합계를 돌려주기.** 엔진의 재조정이 남의
  물량을 자기 것으로 채택하고, 다음 손절이 그것까지 팝니다.
- **안전 분기를 `isinstance(brokerage, LiveBrokerage)` 로 되돌리기.** 그 검사는
  두 질문을 한꺼번에 답합니다 — "계좌 자본을 채택해도 되는가"(슬리브는 아니오)와
  "주문이 진짜 증권사로 나가는가"(슬리브도 예). 뒤쪽에 호가 갱신·체결 폴링·**봉
  사이 손절 재평가**·제출 관문이 매달려 있어서, 나누지 않으면 일봉 전략의 손절이
  하루 한 번만 평가됩니다. 뒤쪽 질문은 `Brokerage.venue_backed` 입니다.
- **증권사 어댑터에 에이전트의 `Portfolio` 넘기기.** 어댑터는 `_sync_once` 에서
  **계좌 전체** 수량과 현금을 자기 장부에 적습니다. 그것이 어느 에이전트의
  장부면 계좌 합계가 거기 들어앉고, 그 에이전트의 손절 하나가 계좌를 비웁니다.
  합계 불변식도 이것을 못 잡습니다 — 나간 수량이 발주자에게 귀속되므로 Σ 는
  맞습니다. `registry.build_account_venue` 가 전용 장부를 줍니다.
- **에이전트마다 `StateStore` 생성.** `_claim()` 의 advisory lock 은 open file
  description 단위라 **같은 프로세스 안에서 자기 자신에게** 잠깁니다(errno 35).
  그룹이 하나 열고 `agent_view()` 로 나눠 주세요.
- **계좌 한도를 에이전트 한도의 합으로 정의.** 방어선이 봇 수만큼 곱해집니다.
  넷이 각자 10만원 한도를 지켜도 계좌는 40만원을 잃습니다.
- **계좌 원장을 `day_budget` 에 넣기.** PK 가 `(run_id, day)` 인데 계좌 한도에는
  run 이 없고, 가짜 run 행을 만들면 그것이 `day_budget JOIN runs WHERE
  mode='live'` 스캔에 걸려 **Toss 계좌 게이트가 모든 시작을 영구히 거절합니다.**
  `account_budget(day, mode)` 가 따로 있습니다.
- **슬리브 원장을 메모리에만 두기.** 재시작 하나가 **모든 보유를 영구히 팔 수
  없게** 만듭니다 — 빈 원장으로 뜨면 계좌 전부가 미귀속이 되고(미귀속은 아무도
  팔 수 없습니다), 에이전트 장부는 `positions` 에서 정상 복원되므로 화면에는
  포지션이 그대로 보이며, 손절은 `min(장부, 원장)` 이 0 을 골라 거절됩니다.
  그리고 합계 불변식은 그 상태를 정상으로 읽습니다.
- **미귀속 채택을 매 시작마다.** `미귀속 := 증권사 − Σ슬리브` 는 불변식을 언제나
  참으로 만듭니다 — 누가 우리 물량을 옮겼다는 사실을 매 시작마다 지우는 셈입니다.
- **`require_trader()` 가 그룹에서 조용히 하나 고르기.** `close_all` 이 그 하나만
  정리하고 성공을 돌려주면, 사용자는 전부 정리된 줄 알고 화면을 닫고 나머지
  셋은 그대로 시장에 남습니다. 여럿이면 `AgentRequired`(400)로 되묻습니다.
- **화면에서 `withAgent()` 없이 돈 움직이는 경로 부르기.**
  `tests/test_agent_ui.py` 가 이것만 따로 검사합니다.

### 알려진 한계

`order_agent`(주문→에이전트 귀속)의 실제 적용 범위는 좁습니다. 체결은 증권사
어댑터의 주문 표에서 나오고 그 표는 재시작하면 비어 있으며, 토스 실거래는
소유권을 확인할 수 없는 미체결 주문이 있으면 `connect()` 에서 **아예 연결을
거부합니다**(그쪽이 더 강한 보호입니다). 이 표가 일하는 자리는 같은 프로세스
안의 재연결, 모의·페이퍼 경로, 그리고 주문 표를 되살리는 어댑터가 생길 때입니다.

공매도는 그룹에서 **거절합니다.** a1 이 10주를 공매도하고 a2 가 10주를 들고
있으면 증권사 순수량은 0 이고, 합계 불변식은 성립하지만 a2 의 10주는 계좌에
없습니다. 조용히 허용한 결과는 "공매도가 된다" 가 아니라 "한 에이전트가 다른
에이전트의 주식을 소비한다" 입니다. 토스·한국투자 현금계좌는 공매도를 주지
않으므로 지금 잃는 기능도 없습니다.

---

## 4. 아직 확인 못 한 것

프로덕션의 **읽기 전용 계좌 조회**에서는 Toss가 내려준
`cashBuyingPower`를 `cash_buying_power`로 읽고, 보유 평가액과 합쳐
`investable_assets`를 만드는 경로를 확인했습니다. 그때 관측한 계좌 금액은
스냅샷일 뿐이므로 문서나 설정에 고정하지 않습니다. 반면 실제 주문·체결
응답은 호출하지 않았고, `toss_broker.py`의 `_FIELDS`가 한때 추정으로 전부
틀렸던 전례가 있으므로 주문 wire 계약은 여전히 미검증입니다.

같은 이유로 이것들도 미검증입니다:
- 수급(`/api/v1/stocks/{symbol}/investor-trading`) 응답의 실제 모양
- 종목명(`/api/v1/stocks`) 다건 조회
- 보유 종목이 1개 이상일 때 계좌(`/api/v1/holdings`) 항목의 실제 모양
- 주문 응답의 `execution.commission`/`execution.tax` (지금은 우리 비용
  모델로 **추정**해서 기록합니다 — 우대 요율 계좌면 어긋납니다)

---

## 5. 설계만 하고 구현 안 한 것

`docs/cross_market.md` — 미국+국내+코인 동시 운용.

`origin/pending/fx-layer`는 이 설계의 1단계를 시도했지만 3라운드 검증에서
반박됐습니다. **구현으로 쓰거나 적용하지 마세요.** 통화 혼합 위험을 다시 다룰
때는 `docs/pending_review.md`의 반박부터 읽고, 현재 main에서 독립 재현한 뒤 새
스키마·이관 범위를 설계해야 합니다.

2~4단계(`CompositeBrokerage`, 종목별 캘린더, 설정 스키마)는 미착수입니다.

---

## 6. 배포

`origin/pending/*` 아홉 브랜치는 전부 반박됐습니다. **머지·cherry-pick하지
마세요.** 먼저 `docs/pending_review.md`의 해당 절과 재현값을 읽어야 합니다.

정적 UI(`quant/api/static/*`)만 바뀐 경우에는 검증된 정확한 SHA를 서버의
`main`에 `git merge --ff-only <SHA>`로 반영합니다. 이 경로는 Python 프로세스를
재시작하지 않습니다. 배포 전후 `quant`의 PID·시작 시각·`NRestarts`와
`/api/health.started_at`이 모두 그대로인지 확인합니다.

아래 설치 스크립트는 최초 프로비저닝이나 Python 의존성·서비스 정의 변경 때만
쓰는 재시작 경로입니다. **평상시 정적 UI 배포 명령이 아닙니다.** Python 코드를
배포해 재시작해야 할 때도 먼저 봇이 꺼져 있고 장이 닫혔는지 확인합니다. 새
일일 한도 계약이 재시작을 fail closed하더라도 장중 배포를 안전하다고 뜻하지는
않습니다.

```bash
sudo bash /home/quant/app/deploy/install.sh
```

이 스크립트는 코드를 당기고 의존성을 맞춘 뒤 서비스를 재시작합니다.
`QUANT_SECRET_KEY` 는 절대 다시 만들지 않습니다 — 그 값을 갈아엎으면 저장된
증권사 키를 전부 못 읽습니다.

Python 코드까지 바꿔 의도적으로 재시작한 경우에만
`https://siftai.kr/api/health`의 `started_at`이 배포 시각보다 뒤여야 합니다.
정적 UI 배포에서는 반대로 `started_at`이 바뀌면 안 됩니다.


## 화면 디자인 (2026-09-03, 토스식)

`quant/api/static/app.css` 를 처음부터 다시 썼습니다 — 흰 카드, 파랑 강조 하나,
Pretendard, 테두리 대신 여백. 픽셀 서체·금색 판은 없습니다(아바타 스프라이트만
픽셀). 토큰 **이름**(`--gold`, `--plate`, `--px` …)은 그대로이고 값만 바뀌었습니다
— chart.js 와 스크립트가 그 이름으로 읽습니다.

지켜야 하는 것: 900px 이하 규칙은 파일 맨 아래 `@media(max-width:900px){` 블록
**하나** 에만 둡니다(검사가 마지막 블록을 찾습니다). 휴대폰에서 페이지 탭은
`.top-in>.pagetabs` 를 `position:fixed` 로 아래에 붙인 것이라, 머리말 격자
계약(3행·`grid-row:3`)은 그대로 만족합니다. `tests/test_ui_readability.py` 가
대비·닿는 높이·격자를 파싱해서 잽니다 — 색을 바꾸면 그 검사부터 돌리세요.

### 2차 검토 (2026-09-04) 가 찾아 고친 것

- **실제 어댑터(KIS·토스)에서는 그룹이 아예 켜지지 않았다.** 잔고를 `balances()`
  로만 읽었는데 그쪽은 기본 구현 `{}` 다. 이제 `AccountGateway.read_account_cash()`
  가 `balances()` → `_venue_capital()['cash']` → `_venue_cash()` 순으로 읽는다.
  가짜 증권사에만 `balances()` 가 있어 테스트만 통과했던 것 — 새 어댑터 관련
  테스트는 반드시 `LiveBrokerage` 를 부모로 두어라.
- 시장가 주문 가격은 **주문 낸 에이전트의 장부** 부터 본다(`_last_price(symbol,
  prefer)`). 형제의 어제 종가로 한도를 매기면 헐거워진다.
- 종료 확인의 원격 미결 수는 **내 몫만** 센다 — 형제의 것으로 아는 미결을 뺀다.
  증권사 응답에는 주인이 없으므로(토스는 clientOrderId 를 주지 않는다) 주인
  모를 주문은 남은 것으로 친다. 그룹의 마지막 에이전트는 계좌 전체를 본다.
- 그룹 정지는 취소된 에이전트의 `Engine.stop` 이 끝날 때까지
  (`GroupTrader.CANCEL_FLUSH_SECONDS`) 기다린 뒤 계좌 연결과 DB를 놓는다.
- `agent_id` 없는 조회는 `Desk.default_agent_id(strategy)` — 템플릿 id 가 맞는
  에이전트, 없으면 첫 에이전트. 돈을 움직이는 경로는 여전히 `AgentRequired`.
- 디자인: `--gold` 는 호박색(차트 진입가·지정가선, "축소"), 901~1100px 에서
  잔고 알약은 둘째 줄, 차트 격자·축 글자 진하게, PWA 아이콘 새 팔레트.

### 최종 점검 (2026-09-04 오후) 가 찾아 고친 것

네 갈래로 나눠 읽었습니다(게이트웨이·그룹 / 어댑터 / 트레이더·한도·상태 /
API·화면). 공통점은 **가짜 증권사로는 절대 드러나지 않는다** 는 것 — 실제
어댑터는 `positions()`·`balances()` 가 `{}` 이고, 인자 없는 `sync()` 가
아니며, 자기 장부를 스스로 적지 않습니다. 새 어댑터 관련 테스트는 반드시
`LiveBrokerage` 를 부모로 둔 대역으로 쓰세요(`tests/test_final_sweep_fixes.py`
의 `RealAdapter`).

**그룹 + 실제 어댑터 (`gateway.py`·`group.py`·`sleeve.py`)**

- **계좌 장부에 아무도 체결을 적지 않았다.** 단일 봇은 어댑터 장부 = 엔진
  장부라 엔진이 적어 주지만, 그룹에서는 어댑터가 전용 장부를 받고 엔진들은
  각자 자기 장부에만 적습니다. 그래서 토스의 매도 보유 검사가 0 을 보고 **모든
  매도를 공매도로 거절** 했고(손절이 한 번도 못 나감), 계좌 자본 확인은
  "체결이 장부에 없다" 로 영영 실패해 신규 매수가 막혔습니다. 이제
  `AccountGateway.settle()` 이 실제 어댑터(`_venue_positions` 계약이 있는 쪽)의
  장부에도 적습니다. 페이퍼는 자기가 적으므로 건드리지 않습니다.
- 불변식의 오른쪽은 어댑터 **장부** 가 아니라 `_venue_positions()`(직접) 또는
  `sync()` 보고서의 `venue_positions` 입니다. 장부는 어댑터가 이름을 아는
  종목만 적어서, 사용자가 앱에서 산 보유를 시작 때 채택하지 못했습니다.
- 실패한 동기화(보고서에 스냅샷 없음)와 미결 봇 주문이 있는 동안은 불변식을
  확인하지 않습니다 — 낡은 장부·"비우기와 조회 사이의 체결" 이 정지로 읽혔고,
  그 정지는 나가는 주문도 막습니다.
- `SleeveBrokerage.sync(**kwargs)`: `LiveTrader._run_exit_safety` 가 넘기는
  `expected_positions`·`independent_position_keys` 를 못 받아 **손절 경로가
  TypeError 로 죽었습니다.** 게이트웨이가 에이전트 기준 기대 수량을 계좌
  기준(형제 슬리브 + 미귀속)으로 옮겨 넘깁니다.
- `SleeveBrokerage.submit` 은 거절을 예외가 아니라 REJECTED 로 돌려줍니다.
  엔진의 `_submit` 은 묶음을 순서대로 보내며 예외를 잡지 않아, 원장에 없는
  종목 하나가 **같은 묶음의 형제 손절** 을 막았습니다.
- 그룹 시작 경쟁: `start()` 가 증권사에 연결하는 동안 `alive` 가 거짓이라 두
  번째 시작 요청이 레지스트리 자리를 덮어쓰고 실패했고, 첫 그룹은 실거래를
  계속하는데 API 에서는 사라져 멈출 손잡이가 없었습니다. `_starting` 동안
  `alive` 는 참이고, 실패 정리는 자기 객체일 때만 자리를 비웁니다.
- 죽은 그룹은 최상위 `error`·`stopped_at` 을 답합니다(`/api/health.last_error`).

**어댑터 (`toss_broker.py`·`kis_broker.py`·`live_base.py`)**

- 토스 `execution.commission`/`tax` 가 `null` 이면 **0 이 아니라 설정의 비용
  모델로 추정** 합니다(4번 절의 "추정해서 기록합니다" 는 이번에야 사실이 됨).
  `"0"` 은 그대로 0. 추정한 주문은 `order.meta["fee_estimated"]` 와 누계를 남기고,
  실계좌 현금 증명은 그 합의 2배만큼 허용폭을 넓힙니다 — 안 넓히면 1원 오차로
  증명이 재시작 전까지 영영 실패해 모든 매수가 막힙니다. 한 주 값이 추정
  비용의 2배보다 싼 동전주에서는 이 폭이 한 주 미체결을 가릴 수 있습니다.
- KIS `_number()` 는 읽을 수 없는 값을 0 으로 지어내지 않고 필드 이름을 대며
  거부합니다. 체결 폴링은 그 행을 `fill_channel_down` 으로, 미결 조회는
  예외로(종료 확인 fail closed).
- 원가를 모르는 증권사 보유는 들이지 않고 `uncorrected` 에 남기며, 그 종목에는
  **신규 진입만** 거부합니다(`_guard`). 줄이는 주문은 막지 않습니다.
- `max_order_notional` 은 신규 노출에만 겁니다. 상한보다 커진 포지션을 손절할
  길이 없었습니다.

**상태 저장소 (`state.py`)**

- 당일 재시작: 형제 에이전트가 오늘 쓴 `day_budget` 을 "전략을 바꿔 한도를
  우회하려는 남" 으로 읽어, 형제 하나가 거래한 순간 장중 재시작이 전원을
  `daily_budget_strategy_switch_blocked` 로 죽였습니다. `StateStore.group_agent_ids`
  (그룹이 저장소를 연 직후 넣음)에 있는 **다른** 에이전트의 행만 면제합니다.
  그룹에서 뺀 에이전트·단일 봇 시절의 빈 agent_id 행은 여전히 남입니다.
  `required`(격리) 판정에는 적용하지 않습니다.
- 같은 템플릿의 두 에이전트가 함께 격리되면 오래된 쪽을 영영 보관할 수
  없었습니다(head 만 보관 가능). 복구 카드는 남은 격리 실행을 차례로 보여 주고,
  보관은 run id 로 찾습니다. 나머지 검사는 그대로입니다.
- 아직 남긴 것: `start_run` 안쪽의 방어 게이트는 `resume_agent_id` 없이 불러,
  형제가 거래한 뒤 **새로** 넣은 에이전트는 다음 KST 날까지 거부됩니다(fail
  closed). `Registry._assert_recovery_start_allowed` 는 단일 봇 경로만 쓰는데,
  그룹에 붙이려면 `group_agent_ids` 와 각 에이전트 id 를 넘겨야 합니다.

**API·화면 (`server.py`·`index.html`·`configs/us_toss*.yaml`)**

- 에이전트 둘 이상인 그룹에서 `agent_id` 없는 **조회** (`/api/candles`·
  `market/snapshot`·`lookup`·`account/broker`·`evaluate`·`desk`·`flow`)가
  프로세스 기본 템플릿(운영은 데모)으로 물러섰습니다 — 차트가 "이 전략의
  종목이 아닙니다", 데스크가 "없음". 조회는 전략 이름이 맞는 에이전트, 없으면
  첫 에이전트를 봅니다. 돈이 움직이는 경로는 여전히 `AgentRequired`.
- `/api/trader/sync` 에 `agent_id` 가 없어 그룹에서는 영영 400 이었습니다.
- 그룹 정지(`status.account.halted`)를 화면이 읽지 않아 실거래 배지가 켜진 채
  모든 주문이 거절됐습니다. 이제 `#runMsg` 와 안전 라벨에 사유가 뜹니다.
- **미국 전략의 하루 한도 시간대를 9 → -5 로.** 한국시간 자정은 뉴욕 개장
  30~90분 뒤라, 09:45 ET 에 손실 한도로 멈춘 봇이 몇 분 뒤 새 허용치를
  받았습니다. `tests/test_us_daily_cap_resets_off_session.py` 가 2026년 매일
  검사합니다. **배포 영향:** 한도 원장은 `(mode, day, tz)` 로 이어지므로 tz 9
  로 적힌 진행 중 미국 원장은 첫 -5 날로 중단을 넘기지 못합니다. 장 밖에서
  배포하세요(0번 절 규칙 그대로).

**확인하지 못한 것 (실제 호출 없이는 못 봄)**

- 토스 `POST /orders` 의 애매한 실패(전송 오류·5xx·깨진 2xx)에 같은
  `clientOrderId` 로 한 번 재시도합니다. 코드는 "공식 10분 멱등 창" 을
  전제하는데 저장소 안에서는 그 근거를 확인할 수 없었습니다. 멱등이 아니면
  주문이 둘이 됩니다(그룹은 불변식이 잡고, 단일 봇은 조용히 두 배 포지션).
  첫 실거래 전에 **1주 스모크 테스트** 를 권합니다: 시장가와 먼 지정가 →
  목록 OPEN → 상세 → 취소 → 상세, 그리고 시장가 매수+매도에서
  `commission`/`tax` 가 `null` 이 아닌지와 `cashBuyingPower` 변화가 장부 수수료와
  맞는지.
- `_pre_venue_submit` 은 매 주문(청산 포함) 전에 미결 목록의 행에 상세 응답과
  같은 스키마(`execution`·`orderedAt`·`timeInForce`·`currency`)를 요구합니다.
  목록 행의 모양이 다르면 봇 주문 하나가 걸려 있는 동안 **손절까지** 거절됩니다.
- KIS 국내 잔고 조회는 첫 페이지만 읽습니다(`CTX_AREA_NK100` 미추적). 보유
  종목이 한 페이지를 넘으면 뒤 종목이 로컬에서 평탄화되고 엔진이 재진입합니다.

---

## 전체 점검 (2026-09-06) 이 찾아 고친 것

여덟 갈래로 나눠 읽었습니다(그룹·게이트웨이 / 어댑터 / 트레이더·한도·상태 /
API·인증 / 엔진·실행 / 데이터·데스크 / 화면 / 규모 구조). 아래는 **재현
테스트로 확인한 뒤 고친 것** 만입니다. 전체 테스트 2,125건 → 2,195건.

**공통점이 다시 한 번 같았습니다:** 결함은 전부 조용합니다. 예외도 로그도
없이 손절이 안 나가거나, 지어낸 숫자로 판단하거나, 멀쩡한 보유를 팝니다.
그리고 절반은 **가짜 증권사로는 드러나지 않습니다** — 새 테스트는 반드시
`venue_capital_truth=True` 인 `LiveBrokerage` 대역으로 쓰세요
(`tests/test_group_starts_with_holdings.py` 의 `TruthVenue`).

### 치명 — 실거래 그룹이 아예 켜지지 않았다

`registry.build_account_venue` 가 주는 계좌 장부는 **빈 `Portfolio`** 이고
아무도 종목을 마킹하지 않습니다. 그래서 계좌 진실 어댑터(토스 실거래)의 첫
`sync()` 가 증권사 보유를 전부 "symbol is not mapped" 로 읽고 `connect()` 가
예외를 냈습니다. **계좌에 주식이 하나라도 있으면 그룹이 시작되지 않는다** 는
뜻이고, 어제 그룹이 산 물량도 마찬가지라 포지션이 생긴 뒤에는 재시작이
불가능했습니다 — 그 포지션은 손절도 청산도 닿지 않습니다.

`AccountGateway.connect()` 가 연결 **전에** 모든 에이전트 유니버스의 종목을
계좌 장부에 등록합니다(`_register_account_symbols`, 수량 0). 유니버스 밖 보유는
예전 정책 그대로 거부합니다 — 이 수정이 넓힌 것은 "우리가 아는 종목인데 장부가
몰랐던" 경우뿐입니다.

### 치명 — 휴장 중 심의가 데스크 지표를 오염시켰다

`_closed_market_deliberation` 은 `closed_cadence_minutes` 마다
`desk.update(ctx, {sym: history[-1]})` 를 부르는데, 그 봉은 **이미 넣은 봉**
입니다. `TradingDesk.update` 는 확인 없이 다시 지표에 넣었습니다. 주말 48회면
20봉 수익률과 변동성이 0 에 수렴하고, 월요일 브리프가 그 숫자를 "결정론적으로
계산된 사실" 로 좌석들에게 건넵니다. LLM 은 캐시라 **비용·로그 어디에도 흔적이
없습니다.** 미국 데스크 봇은 매일 밤 겪습니다.

`TradingDesk._ingested`(심볼별 마지막 봉 시각)로 거릅니다. 새 봉이 하나도
없으면 `[]` 를 돌려주고 `_bar_count`·history·기억·이벤트 어디에도 손대지
않습니다 — 부르는 쪽은 이미 "이 봉은 이미 심의했습니다" 를 띄웁니다.

### 캘린더 표에 **거래일이 휴장으로** 적혀 있었다

표에 잘못 들어간 날은 하루 종일 손절·트레일링이 평가되지 않고 사람이 누른
매도도 다음 개장까지 대기합니다(`_wait_for_market` → `_maintenance_cycle`).

- 2026 `06-08` 제거 — 현충일(토)은 **대체공휴일 대상이 아닙니다.**
- 2026 `09-28` 제거 — 추석 연휴가 토요일까지라 대체가 없습니다. **설·추석은
  일요일 겹침만** 대체 사유입니다(국경일은 토·일 모두).
- 2026 `07-17` 추가 — 제헌절이 공휴일로 되살아났고 KRX 도 휴장을 공표.
- 2027 `02-10`·`06-07` 제거, `12-27` 추가. `US_EARLY_CLOSES[2027]` 의 `07-02`
  제거(독립기념일이 일요일이라 그 전 금요일은 정규장).

규칙 자체를 `tests/test_calendar.py` 가 날짜로 검사하고, 2026 개수(17일)도
함께 봅니다. **표를 갱신할 때 그 테스트부터 읽으세요.**

### 손절이 나가지 않는 경로 넷

- **유지 주기의 예외가 에이전트를 끝냈습니다.** 수동 주문만 try 로 감싸져
  있어서, 토스가 주문 상세를 한 번 못 읽거나(`settle_live_fills` 는 그 실패를
  일부러 다시 던집니다) 정지된 그룹이 `GroupHalted` 를 올리면 그 예외가
  `run()` 까지 올라가 `shutdown()` 을 불렀습니다. 이제 단계마다 `_guarded` 를
  지나고, 사유는 `status().maintenance_error` 로 화면까지 갑니다.
- **`GroupHalted` 를 거절로 접습니다**(`SleeveBrokerage.submit`). 정지된 계좌가
  주문을 안 받는 것은 설계대로지만, 예외로 알리면 `Engine._submit` 이 묶음째
  터지고 에이전트가 죽어 **멈출 손잡이까지** 사라집니다.
- **정산이 끝나지 않은 스냅샷으로 불변식을 봤습니다.** 어댑터가 `ok: False` 와
  `transient` 를 달아 "이번 동기화는 정산 전" 이라고 말하는데도 그 보고서의
  `venue_positions` 로 대조해 그룹을 멈췄고, 정지는 sticky 라 보유가 따라잡아도
  안 풀렸습니다. 이제 `ok is False` 인 보고서의 스냅샷은 쓰지 않습니다(`ok` 키가
  없는 페이퍼·대역은 예전대로).
- **체결 채널 잠금이 영구였습니다.** 토스 주문 POST 가 두 번 애매하게 실패하면
  채널을 잠그는데 그 주문은 로컬에 남지 않아 `fill_channel_up()` 이 영영
  불리지 않았습니다. 이제 추적할 주문이 없고 채널이 잠겨 있으면 미결 목록을
  물어(`_try_recover_fill_channel`) 모르는 주문이 없을 때 풉니다.

### 돈이 잘못 움직이던 것 셋

- **잠금 중 "추가 매수 차단" 이 다음 봉에 보유를 전량 청산했습니다.**
  `Engine.on_bars` 가 리스크 축소 판정을 **제안치** 와 비교해서, 목표를 현재
  수량으로 낮추는 `lock_gate` 의 `add blocked` 를 축소로 읽고 인사이트를
  지웠습니다. 기준을 **보유 수량** 으로 바꿨습니다. 알림(RISK_ACTION)은 깎인
  모든 경우에 그대로 내보냅니다.
- **사용자의 출금이 낙폭으로 읽혀 킬스위치가 전량 청산했습니다.**
  `adopt_venue_capital` 이 현금을 덮어쓰면서 입출금을 구분하지 않았습니다.
  이제 거래로 설명되지 않는 현금 차이를 `external_cash_flow` 로 보고 기준선과
  최고점을 **함께** 옮깁니다. 단, 미결 주문·미증명 체결·끊긴 체결 채널이
  하나라도 있으면 그렇게 읽지 않습니다 — 못 본 체결을 입출금으로 읽으면
  킬스위치가 눈을 감습니다. **주식 입출고는 잡지 못합니다**(현금이 아니라
  수량이 움직임).
- **성향 저장이 설정에 명시된 한도를 풀었습니다.** 실행 중 경로가 무조건
  대입했고 그 본문이 서버와 레지스트리에 **복제** 돼 있었습니다. 이제
  `quant.live.profile.apply_profile_to_engine` 한 곳이고, 런타임에서는 **조이는
  방향만** 반영합니다. 푸는 것은 설정 화면의 하루 한도로 가야 합니다.

### 어댑터

- **KIS 잔고·체결 조회가 첫 장만 읽었습니다**(`CTX_AREA_NK100` 미추적). 뒤 장의
  보유는 `_sync_once` 가 "외부 청산" 으로 읽어 로컬 수량을 0 으로 만들고
  **원가만큼 현금을 지어냅니다.** `KisBrokerage._paged` 가 `tr_cont` 와 이어받기
  키를 따라 끝까지 읽고, **부분 결과는 돌려주지 않습니다**(상한 20장, 넘으면
  예외 — 절반만 아는 잔고보다 조회 실패가 낫습니다).
- **KIS 주문 POST 의 애매한 실패**(전송 오류·5xx·깨진 응답)가 잠금도 표식도 없이
  REJECTED 로 접혔습니다. 다음 봉이 같은 주문을 또 보내고, 지정가면 둘 다
  체결되어 노출이 두 배가 됩니다(KIS 는 취소 미구현). 이제 채널을 잠급니다.
  4xx 는 서버가 읽고 거절한 것이라 그대로 둡니다.

### 화면·서버

- 죽은 그룹이 **살아 있는 단일 봇** 을 `status()` 에서 가렸습니다. 화면은 정지
  버튼을 감추고 `adoptAgents` 가 죽은 에이전트 id 를 모든 돈 경로에 붙여
  매도·청산이 전부 404 였습니다.
- 봇이 멈춰도 머리말 `#mode`·`#strategy` 배지가 "실거래 / 전략명" 그대로였습니다
  (`clearBotBook` 이 함께 내립니다).
- 실거래 확인 창의 하루 한도가 **템플릿 원값** 이었습니다. 실행에는 사용자가
  저장한 값이 걸립니다(`_effective`). `/api/strategies` 가 실제 걸릴 값을
  답합니다(`_effective_limits`).
- 복구 카드의 "봇 실행 중" 판정이 2+ 에이전트 그룹을 못 봤습니다
  (`registry._anything_running`).
- 유지 주기 실패 사유를 안전 라벨이 읽습니다 — 그 동안은 손절이 평가되지
  않는다는 뜻이라 화면이 반드시 말해야 합니다.

### 지나가다 고친 잠재 결함 둘

- `backtest/metrics.py` 의 `tail_ratio` 가 분모 0 에서 **백테스트 전체를**
  ZeroDivisionError 로 끝냈습니다. 성적표 한 칸 때문에 결과를 잃습니다.
- `data/providers/kis.py` 가 모듈 로드 시점에 `asyncio.Lock()` 을 만들어,
  import 순서에 따라 `build_engine` 안에서 터졌습니다. `LazyLock` 으로.

### 이어서 고친 것 — 원장·직렬화·계량·백프레셔·종료·노화

**원장이 사람의 정상적인 행동을 사고로 읽던 것 둘** (`quant/live/gateway.py`)

- **사용자가 자기 주식을 팔면 이후 모든 시작이 영구히 거부** 됐습니다. 미귀속은
  정의상 사용자가 자기 뜻대로 사고파는 물량인데, 그것이 줄어든 것을 "원장이
  주장하는 보유가 계좌에 없다" 로 읽었습니다. 저장된 미귀속은 그대로 남으므로
  다시 시도해도 같은 결과였고, 원장을 되돌릴 화면도 API 도 없었습니다. 이제
  부족분을 **먼저 미귀속에서** 깎고, 그것으로도 모자랄 때 — 즉 **봇이 산
  물량** 이 사라졌을 때 — 만 멈춥니다.
- **에이전트를 뺐다가 다시 넣으면 그 보유가 영영 미귀속** 이었습니다.
  `agents.py` 가 직접 안내하는 사용법인데, 그 보유가 이름을 잃고 저장에서도
  지워져 돌아오지 않았습니다. 장부는 `positions` 에서 복원되므로 화면에는
  포지션이 보이고 손절만 "보유는 0" 으로 거절됩니다. 이제 `_retired` 가
  **이름을 지켜** 보관하고, 다시 넣으면 그대로 돌려줍니다. 빠져 있는 동안은
  미귀속과 같은 무게이고(아무도 못 팜) 불변식도 그 수량을 압니다.
  `status()["retired"]` 로 화면이 그 사실을 말할 수 있습니다.

**형제의 미결이 내 손절을 막던 것** (`quant/brokerage/live_base.py`)

계좌 진실 어댑터는 같은 종목의 미결 주문이 있으면 추가 주문을 직렬화합니다.
막으려는 것은 **초과 매도** 인데 방향을 가리지 않아, 그룹에서 형제가 걸어 둔
먼 지정가 **매수** 하나가 내 손절을 하루 종일 거절했습니다(게이트웨이는 남의
주문을 보여 주지도 취소하지도 않습니다). 이제 줄이는 주문은 **같은 방향** 의
미결과만 경쟁합니다. 신규 노출은 매수 가능 금액을 소비하므로 예전 그대로
전부와 직렬화합니다.

**봉마다 도는 심의가 계량을 안 거치던 것** (`quant/alpha/desk.py`)

`SpendMeter` 가 `_deliberate_now`(시작·휴장 중)와 `/api/evaluate` 에만 있어서,
정작 돈이 되는 경로 — `on_bars` → `TradingDesk.update` → `deliberate` — 는
요금제를 묻지도 사용량을 적지도 않았습니다. 사용자가 자기 키를 안 넣었으면
운영자 키로 나가고 집계에도 안 잡힙니다. 계량은 이제 **데스크 한 곳** 에
있습니다(`_deliberate_now` 의 사본은 제거 — 두 곳이면 같은 호출이 두 번
청구됩니다). 거절되면 데스크만 쉬고 규칙 알파는 그대로 돕니다 — 출하되는
데스크 설정은 전부 규칙 알파를 함께 두고 있어 그 약속이 참입니다. **데스크만
있는 설정을 새로 만들면 그때는 심의가 멎는 것이 곧 청산이 됩니다.**

**화면 하나가 엔진을 멈추던 것** (`quant/api/server.py` `Hub`)

`Hub.publish` 를 부르는 것은 `EventBus.emit` 이고 그것을 부르는 것은 봉을
처리하는 엔진입니다. 화면마다 `await ws.send_text(...)` 를 했으므로 잠긴 폰
하나가 그 사용자의 봉 처리와 봉 사이 손절을 함께 멈췄습니다(그룹이면 넷).
이제 화면마다 큐와 전용 송신 태스크가 있고, 큐가 차면 **오래된 것부터**
버립니다. 붙이고 떼는 것은 `hub.attach()`/`hub.detach()` 로만 하세요 —
`clients` 를 직접 만지면 송신 태스크가 새거나 안 생깁니다.

**종료 예산** (`quant/cli.py`, `deploy/quant.service`)

uvicorn 에 `timeout_graceful_shutdown` 이 없어 진행 중 요청을 **무기한**
기다렸고, 그 뒤에야 봇들의 마지막 상태 저장이 시작됐습니다. `/api/evaluate` 는
데스크 심의라 120초를 쓸 수 있어 그 한 요청이 `TimeoutStopSec` 을 통째로 먹고
SIGKILL 을 불렀습니다 → 토스 실거래 run 격리. 이제 5초로 자르고, 유닛의
예산은 코드가 쓰는 최악치(80초)보다 크게 150초입니다.
`tests/test_deploy_vps.py` 가 **코드에서 숫자를 읽어** 비교하므로, 유예 시간을
늘리면 그 테스트가 유닛도 함께 고치라고 말합니다.

**주문 노화가 죽은 경로이던 것** (`quant/live/trader.py`, `ccxt_broker.py`)

`review_orders` 는 판정을 내려 `pending_cancellations` 에 담는데 **그 목록을
읽는 코드가 없었습니다.** 토스·한투는 DAY 라 장 마감이 치워 주지만 ccxt 는
GTC 로 남아, 8bp 아래 매수 지정가가 며칠 뒤 **낡은 판단으로** 체결됩니다.
이제 유지 주기가 `_flush_aged_orders()` 로 실제로 뺍니다. 거래소가 스스로
끝낸 주문(canceled/expired/rejected)도 로컬에서 닫습니다 — 열린 채로 두면
`projected quantity` 가 없는 주문을 세어 그 종목의 매매가 조용히 멈춥니다.

### 이번에도 고치지 **않은** 것

규모 확장의 나머지 — 자동 재개 없음, LLM 전역 페이서(키 해시별), 백테스트
GIL 분리, SQLite 쓰기 합치기, 봉 마감 지터, 계좌를 일급 개념으로, 운영자
관측(`/api/admin/bots`·로거에 uid). 전부 **구조 변경** 이고 배포 방식과 운영
절차를 바꿉니다.

그 밖에 KRX 호가단위 사다리, 백테스트 거래수·승률이 조각 단위인 것,
`_fetch_new_bars` 가 밀린 봉 중 최신 하나만 처리하는 것, 그룹으로 돌린
템플릿을 단일 봇으로 시작하면 영구 거절되는 것. 근거와 재현 절차는 점검
보고서에 있습니다.

# secrets
.env
.env.*
!.env.example

# state & results — never commit a live trading database
*.db
investor_profile.json
*.db-journal
*.db-wal
*.db-shm
results/
# 앞의 / 가 없으면 git 은 모든 깊이에서 매칭합니다 — 그래서 첫 소스 패키지인
# quant/data/ 가 통째로 커밋에서 빠졌고, clone 하면 import 조차 안 됐습니다.
/data/
!/data/.gitkeep

# python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/
env/

# archived pre-rewrite code
.legacy_backup/

# tooling / os
.idea/
.vscode/
.DS_Store
*.log

# 검증하며 생기는 일회용 설정 — 저장소에 남길 이유가 없습니다
configs/tmp_*.yaml
.audit_findings.json
